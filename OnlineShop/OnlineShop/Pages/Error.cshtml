@page
@model OnlineShop.Pages.ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Помилка</h1>
@if (Model.ShowRequestId)
{
    <p>Request ID: <code>@Model.RequestId</code></p>
}
