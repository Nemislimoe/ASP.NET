<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Галерея</title>
    <!-- Tailwind CDN. -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 min-h-screen flex flex-col">
    <header class="bg-white shadow">
        <div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
            <a href="/" class="text-xl font-semibold">Module 1-5</a>
            <nav class="space-x-4">
                <a href="/" class="text-sm text-gray-600 hover:text-gray-900">Головна</a>
                <a href="/Gallery" class="text-sm text-gray-600 hover:text-gray-900 font-medium">Галерея</a>
            </nav>
        </div>
    </header>

    <main class="flex-1 max-w-6xl mx-auto px-4 py-8 w-full">
        @RenderBody()
    </main>

    <footer class="bg-white border-t">
        <div class="max-w-6xl mx-auto px-4 py-6 text-sm text-gray-600 text-center">
            © @DateTime.Now.Year Мій сайт. Всі права захищено.
        </div>
    </footer>

    @RenderSection("Scripts", required: false)
</body>
</html>
